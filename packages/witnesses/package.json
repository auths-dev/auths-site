{
  "name": "@auths/witnesses",
  "version": "0.1.0",
  "private": true,
  "description": "The witness directory — the single checked-in source for the conformant-witness list and its live /health probe, read by the explorer (explorer.auths.dev). Source-shipped; consumers transpile via Next's transpilePackages.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
